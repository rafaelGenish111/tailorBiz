# API Configuration
# Production: set to your backend Vercel URL, e.g. https://your-backend.vercel.app/api
# Development: leave empty - uses /api + Vite proxy (avoids CORS). Backend default: port 5001
# If backend runs on 5000, set: VITE_PROXY_TARGET=http://localhost:5000
VITE_API_URL=
