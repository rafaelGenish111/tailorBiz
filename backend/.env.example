# Server (פורט 5001 תואם את ה-Vite proxy כברירת מחדל - מונע CORS)
PORT=5001
NODE_ENV=development
ADMIN_BOOTSTRAP_SECRET=your-admin-secret-here

# MongoDB
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname
MONGO_URI=
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# File Upload
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp

# Client URL
CLIENT_URL=http://localhost:5173

# Features
ENABLE_REMINDERS=true
ENABLE_LEAD_NURTURING=true

# Cloudinary (for file uploads)
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_SECRET=your-api-secret

# OpenAI (for AI features)
OPENAI_API_KEY=your-openai-api-key

# Email Configuration
# Choose provider: gmail, sendgrid, or smtp
EMAIL_PROVIDER=gmail

# Email Sender Details
EMAIL_FROM=noreply@yourdomain.com
EMAIL_FROM_NAME=BizFlow

# Gmail Configuration (if EMAIL_PROVIDER=gmail)
# Note: For Gmail, use App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# SendGrid Configuration (if EMAIL_PROVIDER=sendgrid)
# Get API key at: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=your-sendgrid-api-key

# Generic SMTP Configuration (if EMAIL_PROVIDER=smtp)
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password
